(* A minimal Pascal program demonstrating basic text output.
   Note how Pascal uses (* ... *) or { ... } for comments, predating
   the C-style // and /* ... */ that became common later. *)

program Hello;
begin
    (* WriteLn automatically adds a newline character.
       The parentheses are optional for procedures without parameters. *)
    WriteLn('Hello, World!');
end.
